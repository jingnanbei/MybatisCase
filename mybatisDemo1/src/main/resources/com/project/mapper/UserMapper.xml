<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mapper.UserMapper">
    <select id="findAll" resultType="com.project.pojo.User">
        select * from user
    </select>
    <update id="update" parameterType="com.project.pojo.User">
        update `user`
        set username = #{userName},
            sex = #{sex},
            address = #{address}
        where id = #{id}
    </update>
    <delete id="delete" parameterType="int" >
        delete from user where id = #{id}
    </delete>

    <select id="findById" parameterType="int" resultType="com.project.pojo.User">
        select * from user where id = #{id}
    </select>

</mapper>